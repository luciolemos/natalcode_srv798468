name: PHPUnit

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  phpunit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      XDEBUG_MODE: off
      MIN_STATEMENT_COVERAGE: "5"
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2
          coverage: pcov

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.composer/cache/files
            vendor
          key: ${{ runner.os }}-phpunit-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-phpunit-

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Run PHPUnit with coverage
        run: |
          mkdir -p build/logs
          vendor/bin/phpunit --configuration phpunit.xml --coverage-clover build/logs/clover.xml

      - name: Enforce coverage gate
        run: php scripts/check_coverage_gate.php build/logs/clover.xml "$MIN_STATEMENT_COVERAGE"
