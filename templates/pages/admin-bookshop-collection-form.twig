{% extends 'layouts/admin-dashboard.twig' %}

{% set dashboard_page_kicker = 'Dashboard' %}
{% set dashboard_page_title = bookshop_collection_form_is_edit ? 'Editar coleção da livraria' : 'Nova coleção da livraria' %}
{% set dashboard_page_lead = 'Defina a coleção para vincular corretamente séries e volumes do acervo da livraria.' %}

{% block admin_content %}
  <section class="nc-section" aria-label="Formulário de coleção da livraria" data-aos="fade-up" data-aos-delay="0">
    {% if bookshop_collection_form_errors|length > 0 %}
      <article class="nc-admin-form-errors" role="alert">
        <strong>Corrija os campos abaixo:</strong>
        <ul>
          {% for error in bookshop_collection_form_errors %}
            <li>{{ error }}</li>
          {% endfor %}
        </ul>
      </article>
    {% endif %}

    <div class="nc-actions nc-admin-toolbar" data-aos="fade-up" data-aos-delay="60">
      <a class="nc-btn nc-btn-secondary nc-btn-back" href="/painel">Voltar ao painel</a>
      <a class="nc-btn nc-btn-secondary nc-btn-back" href="/painel/livraria/colecoes">Voltar</a>
    </div>

    <article class="nc-card nc-admin-form-card">
      <form class="nc-admin-form" method="post" action="{{ bookshop_collection_form_is_edit ? '/painel/livraria/colecoes/' ~ bookshop_collection_id ~ '/editar' : '/painel/livraria/colecoes/nova' }}">
        <div class="nc-admin-form-grid nc-admin-form-grid-triplet">
          <div class="nc-admin-form-field">
            <label class="nc-admin-form-label" for="name">Nome da coleção</label>
            <input class="nc-admin-form-input" id="name" name="name" type="text" required placeholder="Ex: A Vida no Mundo Espiritual" value="{{ bookshop_collection_form.name|default('') }}">
          </div>

          <div class="nc-admin-form-field">
            <label class="nc-admin-form-label" for="slug">Slug</label>
            <input class="nc-admin-form-input" id="slug" name="slug" type="text" placeholder="Ex: a-vida-no-mundo-espiritual" value="{{ bookshop_collection_form.slug|default('') }}">
          </div>

          <div class="nc-admin-form-field">
            <label class="nc-admin-form-label" for="is_active">Status</label>
            <select class="nc-admin-form-input" id="is_active" name="is_active" required>
              <option value="" {% if bookshop_collection_form.is_active|default('') == '' %}selected{% endif %} disabled>Selecione status</option>
              <option value="1" {{ bookshop_collection_form.is_active == '1' ? 'selected' : '' }}>Ativa</option>
              <option value="0" {{ bookshop_collection_form.is_active == '0' ? 'selected' : '' }}>Inativa</option>
            </select>
          </div>
        </div>

        <div class="nc-admin-form-field nc-admin-form-field-full">
          <label class="nc-admin-form-label" for="description">Descrição</label>
          <textarea class="nc-admin-form-input" id="description" name="description" rows="4" placeholder="Explique quais volumes ou séries pertencem a esta coleção.">{{ bookshop_collection_form.description|default('') }}</textarea>
        </div>

        <div class="nc-actions nc-admin-form-actions" data-aos="fade-up" data-aos-delay="120">
          <button class="nc-btn nc-btn-primary nc-admin-action-create" type="submit">{{ bookshop_collection_form_is_edit ? 'Salvar alterações' : 'Criar coleção' }}</button>
          <a class="nc-btn nc-btn-secondary nc-admin-action-edit nc-btn-back" href="/painel/livraria/colecoes">Voltar</a>
        </div>
      </form>
    </article>
  </section>
{% endblock %}
